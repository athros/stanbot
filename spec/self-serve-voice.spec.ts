import {SelfServeVoice} from "../src/self-serve-voice";
import * as Discord from "eris";
import { auth } from '../src/config.dist';

describe("Command metadata for commands sent to Stanbot.", () => {
    // Set up test objects
    const selfServeVoice = new SelfServeVoice();

    // Perform actions before each it
    beforeEach(function() {
    });

    it("Implement tests here during refactor. Learn how to mock async calls in TS.", function() {});
});
